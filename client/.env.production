# Baked into the production bundle at build time (public URL, not a secret).
# Dev server keeps using the axios.ts localhost fallback automatically.
VUE_APP_API_BASE=https://portfolio-api-jjva.onrender.com
